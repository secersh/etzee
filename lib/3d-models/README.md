# 3D Models

The 0201 display LED model is intentionally omitted for now.

Add a project-authored or clearly licensed `0201_LED.stp` replacement before restoring the 3D model reference in `lib/leds.pretty/0201_LED.kicad_mod`.
